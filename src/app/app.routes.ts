import { Routes } from '@angular/router';
import { DraftBoardComponent } from './draft-board/draft-board.component';

export const routes: Routes = [
    { path: '', component: DraftBoardComponent }
];
